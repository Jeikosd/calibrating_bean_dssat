# dir_run <- "Runs/"
# dir_experiment <- "data/Experiment_data/Palmira/"

run_dssat <- function(dir_experiment, dir_run){
  
  dir_base <- paste0(dir_run, 'temporal/')
  
  dir_run_id <- make_id_run(dir_base, id_cultivar)
  
  
  
  
}